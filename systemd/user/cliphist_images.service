[Unit]
Description=Clipboard image history daemon for Wayland using cliphist
After=graphical-session.target

[Service]
Type=simple
ExecStart=/bin/sh -c 'wl-paste --type image --watch cliphist store'
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
