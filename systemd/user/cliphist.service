[Unit]
Description=Clipboard history daemon for Wayland using cliphist
After=graphical-session.target

[Service]
Type=simple
ExecStart=/bin/sh -c 'wl-paste --type text --watch cliphist store'
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
